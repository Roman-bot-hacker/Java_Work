package ua.lviv.iot;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class HouseDeviceTest {

    /*private HouseDevice houseDevice = new TV(13,"Name", "Producer", 1500, 32,
                                                true, true);

    @Test
    void switchOff() {
        houseDevice.switchOff();
        assertFalse(houseDevice.getSwitched());
    }

    @Test
    void switchOn() {
        houseDevice.switchOn();
        assertTrue(houseDevice.getSwitched());
    }

    @Test
    public String toString() {
        assertEquals(houseDevice.getType()+" "+houseDevice.getProducer()+" "+houseDevice.getName()
                        +" "+houseDevice.getPower(), houseDevice.toString());
        return houseDevice.toString();
    }

    @Test
    void getType(){
        assertEquals(DeviceType.TV, houseDevice.getType());
    }

    @Test
    void getProducer() {
        assertEquals("Producer", houseDevice.getProducer());
        houseDevice.setProducer("New Producer");
        assertEquals("New Producer", houseDevice.getProducer());
    }

    @Test
    void getPower() {
        houseDevice.setPower(1200);
        int power = houseDevice.getPower();
        assertEquals(1200, power);
    }

    @Test
    void getSwitched() {
        houseDevice.switchOn();
        assertEquals(true, houseDevice.getSwitched());
        houseDevice.switchOff();
        assertEquals(false, houseDevice.getSwitched());
    }

    @Test
    void getName() {
        houseDevice.setName("New Name");
        assertEquals("New Name", houseDevice.getName());
    }
*/
}